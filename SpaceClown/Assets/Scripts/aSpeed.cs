using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class aSpeed : MonoBehaviour
{
    public static float mult; //переменная-множитель для увеличения скорости полета
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
